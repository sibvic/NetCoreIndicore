#pragma once
//TODO: conver
//
//typedef size_t index_t;
//inline bool checkIndexRange(index_t index, index_t max)
//{
//    //note: if ever index_t become a signed type the index >= 0 check SHALL be added
//    return (index < max);
//};